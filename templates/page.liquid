<div class="mx-auto mt-20 flex max-w-prose flex-col gap-8 margin-px-1">
  <h1 class="text-4xl font-medium">{{ page.title }}</h1>
  <div class="flex flex-col gap-4">{{ page.content }}</div>
</div>

<style>
  h3 {
    font-size: 1.5rem;
    line-height: 2rem;
    font-weight: 500;
  }
</style>
